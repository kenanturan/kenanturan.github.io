@import url('https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600&family=Playfair+Display:ital,wght@0,400;0,600;0,700;1,400&family=Michroma&family=Rajdhani:wght@300;400;500;600;700&display=swap');

:root {
  /* Color Palette - Academic & Dark */
  --bg-body: #0f1115;
  /* Deep Charcoal/Night */
  --bg-surface: #1a1d23;
  /* Slightly lighter for cards */
  --bg-surface-hover: #23272e;

  --text-main: #e2e4e9;
  /* Off-white for readability */
  --text-muted: #9ca3af;
  /* Slate grey for secondary text */
  --text-dark: #0f1115;

  --accent-gold: #c5a059;
  /* Muted Gold / Bronze */
  --accent-gold-dim: #8a703d;
  --accent-gold-light: #dec085;

  --border-subtle: rgba(255, 255, 255, 0.08);
  --border-focus: rgba(197, 160, 89, 0.5);

  /* Typography */
  --font-display: 'Playfair Display', serif;
  --font-body: 'Inter', sans-serif;

  /* Spacing */
  --space-xs: 0.5rem;
  --space-sm: 1rem;
  --space-md: 2rem;
  --space-lg: 4rem;
  --space-xl: 8rem;

  /* Layout */
  --container-width: 1200px;
  --header-height: 120px;

  /* Transitions */
  --transition-fast: 0.2s ease;
  --transition-smooth: 0.4s cubic-bezier(0.4, 0, 0.2, 1);
}

/* Reset & Base */
*,
*::before,
*::after {
  box-sizing: border-box;
  margin: 0;
  padding: 0;
}

body {
  font-family: --font-body, system-ui, sans-serif;
  background-color: var(--bg-body);
  color: var(--text-main);
  line-height: 1.6;
  -webkit-font-smoothing: antialiased;
  min-height: 100vh;
  display: flex;
  flex-direction: column;
  opacity: 0;
  animation: fadeIn 0.6s ease-in-out forwards;
}

@keyframes fadeIn {
  from {
    opacity: 0;
  }

  to {
    opacity: 1;
  }
}

img,
svg {
  display: block;
  max-width: 100%;
}

a {
  text-decoration: none;
  color: inherit;
  transition: var(--transition-fast);
}

ul {
  list-style: none;
}

/* Typography Utilities */
h1,
h2,
h3,
h4,
h5,
h6 {
  font-family: var(--font-display);
  font-weight: 600;
  line-height: 1.2;
  color: var(--text-main);
  letter-spacing: -0.01em;
}

h1 {
  font-size: clamp(2.5rem, 5vw, 4rem);
}

h2 {
  font-size: clamp(2rem, 4vw, 3rem);
  margin-bottom: var(--space-md);
}

h3 {
  font-size: 1.5rem;
  margin-bottom: var(--space-sm);
  color: var(--accent-gold);
}

p {
  margin-bottom: var(--space-sm);
  font-weight: 300;
  color: var(--text-muted);
}

.text-gold {
  color: var(--accent-gold);
}

.text-center {
  text-align: center;
}

.text-justify {
  text-align: justify;
}

/* Layout Utilities */
.container {
  width: 90%;
  max-width: var(--container-width);
  margin: 0 auto;
  padding: 0 var(--space-sm);
}

.section {
  padding: var(--space-xl) 0;
}

/* Navigation */
.navbar {
  height: var(--header-height);
  width: 100%;
  position: fixed;
  top: 0;
  left: 0;
  background-color: rgba(15, 17, 21, 0.95);
  backdrop-filter: blur(10px);
  z-index: 1000;
  border-bottom: 1px solid var(--border-subtle);
  transition: transform 0.3s ease-in-out;
  /* Add smooth transition for hiding */
}

.navbar.hidden {
  transform: translateY(-100%);
  /* Slide up to hide */
}

.navbar .container {
  height: 100%;
  display: flex;
  justify-content: space-between;
  align-items: center;
}

.logo {
  display: flex;
  align-items: center;
  padding: 5px 0;
  /* precise adjustment */
}

.logo img {
  height: 100px;
  /* Increased to maximize visibility within 120px header */
  width: auto;
}

.nav-links {
  display: flex;
  gap: var(--space-md);
}

.nav-link {
  font-size: 0.9rem;
  letter-spacing: 0.05em;
  text-transform: uppercase;
  color: var(--text-muted);
  font-weight: 400;
  position: relative;
}

.nav-link:hover,
.nav-link.active {
  color: var(--accent-gold);
}

.nav-link::after {
  content: '';
  position: absolute;
  bottom: -4px;
  left: 0;
  width: 0%;
  height: 1px;
  background-color: var(--accent-gold);
  transition: var(--transition-fast);
}

.nav-link:hover::after,
.nav-link.active::after {
  width: 100%;
}

.lang-switch {
  display: flex;
  gap: 8px;
  align-items: center;
  margin-left: var(--space-md);
  padding-left: var(--space-md);
  border-left: 1px solid var(--border-subtle);
}

.lang-link {
  font-size: 0.85rem;
  color: var(--text-muted);
  font-weight: 500;
  transition: var(--transition-fast);
  text-decoration: none;
}

.lang-link:hover,
.lang-link.active {
  color: var(--accent-gold);
}

.mobile-lang {
  display: none !important;
}

/* Mobile adjustments for lang switch */
@media (max-width: 768px) {
  .lang-switch {
    margin-left: 0;
    padding-left: 0;
    border-left: none;
    margin-top: 10px;
    width: 100%;
    justify-content: center;
  }
}

/* Hamburger Menu */
.menu-toggle {
  display: none;
  flex-direction: column;
  cursor: pointer;
  gap: 6px;
}

.bar {
  width: 25px;
  height: 2px;
  background-color: var(--text-main);
  transition: var(--transition-fast);
}

/* Mobile Navigation State */
@media (max-width: 768px) {
  .nav-links {
    display: none;
    width: 100%;
    flex-direction: column;
    align-items: center;
    background-color: rgba(15, 17, 21, 0.98);
    position: absolute;
    top: var(--header-height);
    left: 0;
    padding: var(--space-md) 0;
    border-bottom: 1px solid var(--border-subtle);
  }

  .nav-links.active {
    display: flex;
  }

  .menu-toggle {
    display: flex;
  }

  .menu-toggle.active .bar:nth-child(2) {
    opacity: 0;
  }

  .menu-toggle.active .bar:nth-child(1) {
    transform: translateY(8px) rotate(45deg);
  }

  .menu-toggle.active .bar:nth-child(3) {
    transform: translateY(-8px) rotate(-45deg);
  }
}

/* Footer */
footer {
  margin-top: auto;
  padding: var(--space-lg) 0;
  border-top: 1px solid var(--border-subtle);
  font-size: 0.85rem;
  color: var(--text-muted);
}

/* Components */
.btn {
  display: inline-block;
  padding: 1rem 2rem;
  border: 1px solid var(--accent-gold);
  color: var(--accent-gold);
  font-family: var(--font-body);
  font-size: 0.9rem;
  letter-spacing: 0.1em;
  text-transform: uppercase;
  transition: var(--transition-smooth);
  background: transparent;
  cursor: pointer;
}

.btn:hover {
  background: var(--accent-gold);
  color: var(--text-dark);
}

.card {
  background: var(--bg-surface);
  padding: var(--space-md);
  border: 1px solid var(--border-subtle);
  transition: var(--transition-smooth);
  height: 100%;
  position: relative;
  /* For absolute positioning of icon */
  overflow: hidden;
  /* Ensures icon doesn't overflow if we make it large/artistic, though for small corner it's fine. Keeps it neat. */
}

.card:hover {
  border-color: var(--accent-gold-dim);
  transform: translateY(-5px);
}

.card-icon {
  position: absolute;
  top: 1rem;
  right: 1rem;
  width: 18px;
  height: 18px;
  color: var(--accent-gold);
  stroke-width: 2;
  opacity: 0.6;
}

.card-emblem {
  position: absolute;
  top: 1rem;
  right: 1rem;
  width: 18px;
  height: 18px;
  color: var(--accent-gold);
  opacity: 0.6;
  pointer-events: none;
  transition: var(--transition-smooth);
  display: flex;
  align-items: center;
  justify-content: center;
}

.card-emblem svg {
  width: 100%;
  height: 100%;
  stroke-width: 2;
}

.card:hover .card-emblem {
  opacity: 1;
  transform: scale(1.1);
  color: var(--accent-gold);
}

/* Premium Shadow Utility */
.shadow-levitate {
  box-shadow: 0 20px 40px -10px rgba(0, 0, 0, 0.5);
  /* Deep, soft shadow */
  transition: transform 0.4s ease, box-shadow 0.4s ease;
}

.shadow-levitate:hover {
  transform: translateY(-5px);
  box-shadow: 0 25px 50px -12px rgba(197, 160, 89, 0.15);
  /* Gold tint on hover */
}



.manifesto-text {
  font-family: var(--font-display);
  font-size: clamp(1.2rem, 3vw, 2rem);
  line-height: 1.4;
  color: var(--text-main);
  max-width: 800px;
  margin: 0 auto;
  text-align: center;
  border-left: 3px solid var(--accent-gold);
  padding-left: var(--space-md);
}

section.hero-section {
  padding-top: calc(var(--header-height) + var(--space-xl));
  min-height: 90vh;
  display: flex;
  align-items: center;
  justify-content: center;
  justify-content: center;
}

section.content-section {
  padding-top: calc(var(--header-height) + var(--space-lg));
  min-height: 50vh;
}

/* Mobile specific */
@media (max-width: 768px) {
  :root {
    --space-md: 1.5rem;
    --space-lg: 2.5rem;
    --space-xl: 4rem;
    --header-height: 100px;
    /* Reduced height for mobile */
  }

  .desktop-only {
    display: none !important;
  }

  section.hero-section {
    padding-top: calc(var(--header-height) + var(--space-xl));
    min-height: 90vh;
    display: flex;
    flex-direction: column;
    /* Disable vertical centering on mobile to prevent top clipping */
  }

  section.content-section {
    padding-top: 220px !important;
    /* Increased to prevent overlap */
  }

  /* When screen is tall enough, we can try to center, but default to safe flow */
  @media (min-height: 800px) {
    section.hero-section {
      justify-content: center;
    }
  }

  .mobile-lang {
    display: block !important;
    margin-top: var(--space-sm);
    text-align: center;
    width: 100%;
    padding-top: var(--space-sm);
    border-top: 1px solid var(--border-subtle);
  }

  .mobile-lang a {
    font-size: 1rem;
    font-weight: 500;
    color: var(--text-muted);
    padding: 0 10px;
  }

  .mobile-lang a.active {
    color: var(--accent-gold);

  }

  .section {
    padding: var(--space-lg) 0;
  }

  /* Navbar Mobile Layout */
  .navbar {
    position: fixed;
    top: 0;
    left: 0;
    width: 100%;
    height: auto;
    /* Allow height to grow with content */
    min-height: var(--header-height);
    /* Ensure it takes up at least the reserved space */
    padding: 15px 0;
    z-index: 1000;
  }

  .navbar .container {
    flex-direction: row;
    /* Keep row for logo and toggle */
    gap: 15px;
    justify-content: space-between;
    flex-wrap: wrap;
  }

  /* Prevent horizontal scroll globally */
  body,
  html {
    overflow-x: hidden;
    width: 100%;
  }

  .logo img {
    height: 60px;
    /* Smaller logo for sticky header */
  }

  /* Nav links are now handled by .nav-links.active above */

  .nav-link {
    font-size: 0.85rem;
    padding: 5px;
  }

  /* Typography Adjustments */
  h1 {
    font-size: 2.5rem;
  }

  h2 {
    font-size: 2rem;
  }

  /* Grid Fixes */
  .leadership-grid,
  .approach-grid,
  .publication-grid {
    display: grid;
    grid-template-columns: 1fr;
    /* Stack vertically */
    gap: var(--space-lg);
  }

  /* Centering images in publication grid on mobile */
  .publication-grid>div:first-child {
    width: 100% !important;
    /* Override inline 200px */
    display: flex;
    justify-content: center;
    margin-bottom: var(--space-sm);
  }

  .publication-grid img {
    max-width: 200px;
    /* Keep reasonable size */
  }

  /* General Touch Targets */
  a,
  button {
    min-height: 44px;
    /* Ensure 44px touch target */
    min-width: 44px;
    display: inline-flex;
    align-items: center;
    justify-content: center;
  }
}

@media (min-width: 769px) {
  .leadership-grid {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: var(--space-xl);
  }

  .approach-grid {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: var(--space-xl);
  }

  .publication-grid {
    display: grid;
    grid-template-columns: 200px 1fr;
    gap: var(--space-lg);
  }
}

/* Social Share Buttons */
.share-action {
  color: var(--text-muted);
  text-decoration: none;
  display: inline-flex;
  align-items: center;
  gap: 8px;
  font-size: 0.9rem;
  transition: color 0.3s ease;
  cursor: pointer;
  border: none;
  background: none;
  padding: 0;
  font-family: inherit;
}

.share-action:hover {
  color: var(--accent-gold);
}

/* Toast Notification */
.toast-notification {
  position: fixed;
  bottom: 30px;
  left: 50%;
  transform: translateX(-50%) translateY(20px);
  background-color: var(--bg-surface);
  color: var(--text-main);
  padding: 12px 24px;
  border-radius: 8px;
  border: 1px solid var(--accent-gold);
  box-shadow: 0 10px 30px rgba(0, 0, 0, 0.5);
  display: flex;
  align-items: center;
  gap: 12px;
  opacity: 0;
  visibility: hidden;
  transition: all 0.4s cubic-bezier(0.175, 0.885, 0.32, 1.275);
  z-index: 2000;
  font-family: var(--font-body);
  font-size: 0.95rem;
  min-width: 300px;
  justify-content: center;
  text-align: center;
}

.toast-notification.show {
  opacity: 1;
  visibility: visible;
  transform: translateX(-50%) translateY(0);
}

.toast-notification svg {
  color: var(--accent-gold);
  width: 20px;
  height: 20px;
  flex-shrink: 0;
}

@media (max-width: 768px) {
  .toast-notification {
    bottom: auto;
    top: 20px;
    width: 90%;
    min-width: auto;
  }
}

/* Portal & Hidden Page Styles */
.portal-link {
  font-size: 0.75rem !important;
  /* Smaller than regular links */
  opacity: 0.5;
  margin-left: var(--space-md);
  letter-spacing: 0.15em;
  transition: all 0.3s ease;
  border-bottom: none !important;
  /* No underline by default */
}

.portal-link:hover {
  opacity: 1;
  color: var(--accent-gold);
  text-shadow: 0 0 8px rgba(197, 160, 89, 0.4);
}

.font-tech {
  font-family: 'Michroma', sans-serif;
}

.font-scifi {
  font-family: 'Rajdhani', sans-serif;
}

/* Login Overlay */
#login-overlay {
  position: fixed;
  top: 0;
  left: 0;
  width: 100vw;
  height: 100vh;
  background: #000;
  z-index: 9999;
  display: flex;
  align-items: center;
  justify-content: center;
  flex-direction: column;
  transition: opacity 0.8s ease, visibility 0.8s ease;
}

#login-overlay.unlocked {
  opacity: 0;
  visibility: hidden;
  pointer-events: none;
}

.login-container {
  text-align: center;
  width: 100%;
  max-width: 400px;
  padding: 2rem;
}

.login-input {
  background: transparent;
  border: none;
  border-bottom: 2px solid var(--text-muted);
  color: var(--accent-gold);
  font-family: 'Michroma', sans-serif;
  font-size: 1.5rem;
  text-align: center;
  width: 100%;
  padding: 10px;
  margin-top: 20px;
  outline: none;
  transition: border-color 0.3s ease;
}

.login-input:focus {
  border-color: var(--accent-gold);
}

.login-message {
  color: var(--text-muted);
  margin-top: 15px;
  font-size: 0.9rem;
  height: 1.5em;
  /* Prevent jump */
}

/* Hidden Page Content Overrides */
.hidden-page-body {
  background-color: #050608;
  /* Darker than normal */
}

.hidden-hero {
  position: relative;
  overflow: hidden;
}

.hidden-hero::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  background-image: url('assets/abstract-concept.png');
  /* Fallback or specific tactical image */
  background-size: cover;
  background-position: center;
  filter: blur(8px) brightness(0.3) grayscale(100%);
  z-index: -1;
}

.tech-grid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
  gap: var(--space-md);
}

.tech-card {
  background: rgba(26, 29, 35, 0.6);
  border: 1px solid rgba(255, 255, 255, 0.05);
  backdrop-filter: blur(10px);
  padding: var(--space-md);
  transition: all 0.3s ease;
}

.tech-card:hover {
  border-color: var(--accent-gold);
  box-shadow: 0 0 20px rgba(197, 160, 89, 0.1);
  transform: translateY(-5px);
}

/* Academy Page Aesthetics */
/* Academy Page Aesthetics - Swiss Minimalist (Fresh Start) */
.academy-hero {
  position: relative;
  padding: calc(var(--header-height) + var(--space-xl)) 0 var(--space-xl);
  text-align: center;
  background: #050505;
  /* Pure, deep black */
  margin-bottom: var(--space-xxl);
  border-bottom: 1px solid var(--border-subtle);
}

.academy-hero h1 {
  font-family: var(--font-display);
  font-size: clamp(2rem, 5vw, 3rem);
  font-weight: 300;
  letter-spacing: -0.01em;
  color: var(--text-main);
  margin-bottom: var(--space-md);
  max-width: 900px;
  margin-left: auto;
  margin-right: auto;
}

.academy-hero p {
  font-size: 1.1rem;
  color: var(--text-muted);
  max-width: 600px;
  margin: 0 auto;
  font-weight: 300;
  line-height: 1.6;
}

/* Minimal Column Layouts */
.minimal-grid-4 {
  display: grid;
  grid-template-columns: repeat(4, 1fr);
  gap: var(--space-lg);
  margin-bottom: var(--space-xxl);
  border-top: 1px solid var(--border-subtle);
  padding-top: var(--space-lg);
}

.minimal-grid-3 {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: var(--space-xl);
  margin-bottom: var(--space-xxl);
  border-top: 1px solid var(--border-subtle);
  padding-top: var(--space-lg);
}

.minimal-col {
  display: flex;
  flex-direction: column;
  gap: var(--space-sm);
}

/* Tiny Header with 16px Icon */
.minimal-col-header {
  display: flex;
  align-items: center;
  gap: 10px;
  margin-bottom: 5px;
}

.minimal-col-header svg {
  width: 16px;
  height: 16px;
  color: var(--accent-gold);
  stroke-width: 1.5;
  flex-shrink: 0;
}

.minimal-col-header h4 {
  font-size: 0.95rem;
  font-weight: 600;
  text-transform: uppercase;
  letter-spacing: 0.05em;
  color: var(--text-main);
  margin: 0;
}

.minimal-col p {
  font-size: 0.9rem;
  line-height: 1.6;
  color: var(--text-muted);
  margin: 0;
  opacity: 0.8;
}

/* Common Section Title for this layout */
.minimal-section-title {
  font-family: var(--font-display);
  font-size: 1.5rem;
  color: var(--text-main);
  margin-bottom: var(--space-md);
  text-align: center;
  font-weight: 400;
  letter-spacing: 0.05em;
  text-transform: uppercase;
  margin-left: auto;
  margin-right: auto;
}

/* Mobile Responsive */
@media (max-width: 768px) {
  .academy-hero {
    padding-top: calc(var(--header-height) + var(--space-lg));
    text-align: left;
  }

  .academy-hero h1 {
    font-size: 2rem;
  }

  .academy-hero p {
    margin: 0;
  }

  .minimal-grid-4,
  .minimal-grid-3 {
    grid-template-columns: 1fr;
    gap: var(--space-xl);
    border-top: none;
    padding-top: 0;
  }

  .minimal-col {
    padding-bottom: var(--space-lg);
    border-bottom: 1px solid var(--border-subtle);
  }

  .minimal-col:last-child {
    border-bottom: none;
    padding-bottom: 0;
  }
}

.tech-stat-icon {
  display: inline-block;
  width: 24px;
  height: 24px;
  margin-right: 8px;
  vertical-align: middle;
  opacity: 0;
  transform: translateY(10px);
  transition: all 0.3s ease;
  color: var(--accent-gold);
}

.tech-card:hover .tech-stat-icon {
  opacity: 1;
  transform: translateY(0);
}

/* Application Form */
.classified-form {
  background: rgba(10, 10, 10, 0.8);
  border-left: 4px solid var(--accent-gold);
  padding: var(--space-lg);
  margin-top: var(--space-xl);
}

.form-group {
  margin-bottom: 2rem;
}

.form-label {
  display: block;
  font-family: 'Rajdhani', sans-serif;
  font-weight: 600;
  color: var(--text-muted);
  margin-bottom: 0.5rem;
  text-transform: uppercase;
  letter-spacing: 1px;
}

.form-input {
  width: 100%;
  background: rgba(255, 255, 255, 0.05);
  border: 1px solid var(--border-subtle);
  padding: 1rem;
  color: var(--text-main);
  font-family: 'Inter', sans-serif;
  transition: all 0.3s ease;
}

/* Shared Minimal Styles for Expertise Shortcuts */
.hover-card {
  transition: transform 0.3s ease, background-color 0.3s ease;
  padding: 1.5rem;
  border-radius: 2px;
  border-left: 1px solid transparent;
  height: 100%;
}

.hover-card:hover {
  background-color: rgba(255, 255, 255, 0.02);
  border-left: 1px solid var(--accent-gold);
  transform: translateX(5px);
}

.hover-card .exp-number {
  font-family: 'Courier New', monospace;
  font-size: 0.8rem;
  color: var(--accent-gold);
  opacity: 0.7;
  letter-spacing: 0.1em;
  margin-bottom: 0.8rem;
  display: block;
}

.hover-card h4 {
  font-family: 'Rajdhani', sans-serif;
  font-weight: 700;
  font-size: 1.1rem;
  letter-spacing: 0.08em;
  color: #fff;
  margin-bottom: 0.8rem;
  line-height: 1.4;
}

.hover-card p {
  font-family: 'Inter', sans-serif;
  font-size: 0.95rem;
  font-weight: 300;
  line-height: 1.7;
  color: #a0a0a0;
  opacity: 0.9;
}

.form-input:focus {
  border-color: var(--accent-gold);
  outline: none;
  background: rgba(255, 255, 255, 0.1);
}

.form-disclaimer {
  background: rgba(197, 160, 89, 0.1);
  padding: 1rem;
  border: 1px dashed var(--accent-gold-dim);
  color: var(--accent-gold);
  font-family: 'Rajdhani', sans-serif;
  margin-bottom: 2rem;
  text-align: center;
}


/* Accordion & Group Styles - Executive Tech */
.group-header {
  margin: var(--space-xl) 0 var(--space-md) 0;
  padding-bottom: 0.8rem;
  position: relative;
  border-bottom: 1px solid rgba(255, 255, 255, 0.1);
  display: flex;
  align-items: center;
}

.group-header h2 {
  font-size: 0.9rem;
  letter-spacing: 0.15em;
  color: var(--accent-gold);
  text-shadow: 0 0 10px rgba(197, 160, 89, 0.3);
  margin: 0;
  position: relative;
  padding-left: 1rem;
}

.group-header h2::before {
  content: '';
  position: absolute;
  left: 0;
  top: 50%;
  transform: translateY(-50%);
  width: 4px;
  height: 4px;
  background: var(--accent-gold);
  box-shadow: 0 0 5px var(--accent-gold);
}

.accordion-item {
  background: linear-gradient(90deg, rgba(255, 255, 255, 0.01) 0%, rgba(255, 255, 255, 0) 100%);
  border-left: 1px solid rgba(255, 255, 255, 0.1);
  border-bottom: 1px solid rgba(255, 255, 255, 0.05);
  margin-bottom: 0;
  transition: all 0.3s ease;
}

.accordion-item:hover {
  border-left-color: var(--accent-gold);
  background: linear-gradient(90deg, rgba(197, 160, 89, 0.05) 0%, rgba(255, 255, 255, 0) 100%);
}

.accordion-header {
  width: 100%;
  text-align: left;
  background: transparent;
  padding: 1.5rem 1.5rem;
  color: var(--text-main);
  font-family: 'Rajdhani', sans-serif;
  font-size: 1.2rem;
  font-weight: 600;
  cursor: pointer;
  border: none;
  display: flex;
  justify-content: space-between;
  align-items: center;
  transition: all 0.3s ease;
  opacity: 0.9;
}

.accordion-header:hover {
  color: #fff;
  opacity: 1;
  padding-left: 2rem;
  /* Indent on hover */
}

.accordion-header::after {
  content: '+';
  font-size: 1.4rem;
  color: var(--text-muted);
  font-weight: 300;
  transition: all 0.3s ease;
}

.accordion-header.active {
  color: var(--accent-gold);
  border-left: 2px solid var(--accent-gold);
  padding-left: 2rem;
}

.accordion-header.active::after {
  transform: rotate(45deg);
  color: var(--accent-gold);
  text-shadow: 0 0 8px var(--accent-gold);
}

.accordion-content {
  max-height: 0;
  overflow: hidden;
  transition: max-height 0.4s cubic-bezier(0.4, 0, 0.2, 1);
  padding: 0 2rem;
  background: rgba(0, 0, 0, 0.2);
}

.accordion-content.active {
  padding-bottom: 2rem;
  padding-top: 1rem;
  border-top: 1px dashed rgba(255, 255, 255, 0.1);
}

/* Internal Grid for Modules */
.module-list {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 2rem;
  margin-top: 1rem;
}

.module-item {
  background: transparent;
  padding: 0;
  border-top: 1px solid rgba(255, 255, 255, 0.05);
  padding-top: 1rem;
  text-align: left;
  border-left: none;
}

.module-title {
  color: var(--accent-gold);
  font-size: 0.7rem;
  font-family: 'Michroma', sans-serif;
  margin-bottom: 0.6rem;
  display: block;
  opacity: 0.9;
  letter-spacing: 0.05em;
}

.module-item p {
  font-size: 0.85rem;
  color: var(--text-muted);
  line-height: 1.6;
  margin: 0;
  font-weight: 300;
}

.pdf-btn {
  display: inline-flex;
  align-items: center;
  gap: 10px;
  font-size: 0.7rem;
  color: var(--text-main);
  border: 1px solid rgba(255, 255, 255, 0.1);
  padding: 8px 16px;
  border-radius: 2px;
  margin-top: 1.5rem;
  letter-spacing: 2px;
  text-transform: uppercase;
  font-family: 'Rajdhani', sans-serif;
  font-weight: 600;
  background: transparent;
  transition: all 0.3s ease;
  opacity: 0.6;
}

.pdf-btn:hover {
  background: var(--accent-gold);
  color: #000;
  border-color: var(--accent-gold);
  opacity: 1;
  box-shadow: 0 0 15px rgba(197, 160, 89, 0.3);
}

@media (max-width: 768px) {
  .module-list {
    grid-template-columns: 1fr;
    gap: 1.5rem;
  }

  .accordion-content {
    padding: 0 1rem;
  }
}

/* --- DASHBOARD / OPERATIONAL MATRIX STYLES --- */

/* Login Refinements */
.login-blur-bg {
  position: absolute;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  background-image: url('assets/abstract-concept.png');
  background-size: cover;
  background-position: center;
  filter: blur(15px) brightness(0.4);
  z-index: -1;
}

.login-input-large {
  font-size: 2rem;
  letter-spacing: 0.5rem;
  border-bottom: 2px solid var(--accent-gold);
  background: transparent;
  color: white;
  text-align: center;
  width: 100%;
  max-width: 500px;
  padding: 1rem;
  font-family: 'Michroma', sans-serif;
  outline: none;
  text-transform: uppercase;
}

/* Matrix Layout */
.dashboard-matrix {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: var(--space-md);
  margin-bottom: var(--space-xl);
}

.matrix-column {
  display: flex;
  flex-direction: column;
  gap: var(--space-md);
}

.column-header {
  text-align: center;
  border-bottom: 2px solid var(--accent-gold);
  padding-bottom: 1rem;
  margin-bottom: 1rem;
}

.column-header h3 {
  font-size: 1.2rem;
  letter-spacing: 0.2em;
  color: var(--accent-gold);
  margin: 0;
}

.column-header svg {
  margin: 0 auto 0.5rem auto;
  display: block;
  color: var(--accent-gold);
  opacity: 0.8;
}

/* Module Cards */
.module-card {
  background: rgba(26, 29, 35, 0.6);
  border: 1px solid rgba(255, 255, 255, 0.05);
  padding: 1.5rem;
  position: relative;
  transition: all 0.3s ease;
  backdrop-filter: blur(5px);
}

.module-card.locked {
  opacity: 0.5;
  border-color: rgba(255, 255, 255, 0.02);
  filter: grayscale(1);
  pointer-events: none;
  /* Optional: disable clicking if it's a link */
}

.module-card.completed {
  border-color: var(--accent-gold-dim);
  box-shadow: 0 0 15px rgba(197, 160, 89, 0.05);
}

.module-card:hover:not(.locked) {
  transform: translateY(-3px);
  border-color: var(--accent-gold);
}

.module-level {
  font-size: 0.7rem;
  color: var(--text-muted);
  text-transform: uppercase;
  letter-spacing: 0.1em;
  margin-bottom: 0.5rem;
  display: block;
}

.module-title {
  font-family: 'Rajdhani', sans-serif;
  font-size: 1.1rem;
  font-weight: 600;
  color: var(--text-main);
  margin-bottom: 1rem;
  display: block;
}

/* Progress Bar */
.progress-container {
  width: 100%;
  height: 4px;
  background: rgba(255, 255, 255, 0.1);
  border-radius: 2px;
  margin-top: 1rem;
  position: relative;
}

.progress-bar {
  height: 100%;
  background: var(--accent-gold);
  border-radius: 2px;
  width: 0%;
}

.progress-label {
  font-size: 0.65rem;
  color: var(--accent-gold);
  display: block;
  text-align: right;
  margin-top: 4px;
  font-family: 'Michroma', sans-serif;
}

/* Status Icons */
.status-icon {
  position: absolute;
  top: 1rem;
  right: 1rem;
  width: 16px;
  height: 16px;
}

/* Elite Performance Card */
.elite-performance-card {
  background: linear-gradient(135deg, rgba(15, 17, 21, 0.95) 0%, rgba(20, 20, 30, 0.95) 100%);
  border: 1px solid var(--accent-gold);
  padding: var(--space-lg);
  margin: var(--space-xl) 0;
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: var(--space-md);
  position: relative;
  overflow: hidden;
  box-shadow: 0 0 30px rgba(0, 0, 0, 0.5);
}

.elite-performance-card::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  width: 4px;
  height: 100%;
  background: var(--accent-gold);
  box-shadow: 0 0 10px var(--accent-gold);
}

.elite-content {
  flex: 2;
  z-index: 1;
}

.elite-action {
  flex: 1;
  text-align: right;
  z-index: 1;
}

.elite-bg-icon {
  position: absolute;
  right: -20px;
  bottom: -20px;
  width: 200px;
  height: 200px;
  color: var(--accent-gold);
  opacity: 0.05;
  z-index: 0;
  pointer-events: none;
}

.btn-elite {
  background: var(--accent-gold);
  color: #000;
  font-weight: 700;
  padding: 1rem 2rem;
  border: none;
  font-family: 'Michroma', sans-serif;
  letter-spacing: 1px;
  cursor: pointer;
  box-shadow: 0 0 20px rgba(197, 160, 89, 0.4);
  transition: all 0.3s ease;
}

.btn-elite:hover {
  transform: scale(1.05);
  box-shadow: 0 0 30px rgba(197, 160, 89, 0.6);
}

/* Secondary Grid */
.secondary-grid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
  gap: var(--space-md);
  margin-top: var(--space-xl);
}

.secondary-card {
  background: rgba(26, 29, 35, 0.4);
  border: 1px solid rgba(255, 255, 255, 0.05);
  padding: 1.5rem;
  display: flex;
  align-items: flex-start;
  gap: 1rem;
  transition: all 0.3s ease;
}

.secondary-card:hover {
  background: rgba(26, 29, 35, 0.8);
  border-color: rgba(197, 160, 89, 0.3);
}

.secondary-icon {
  color: var(--accent-gold);
  opacity: 0.7;
  flex-shrink: 0;
}

.secondary-content h4 {
  font-family: 'Rajdhani', sans-serif;
  color: var(--text-main);
  margin-bottom: 0.5rem;
  font-size: 1.1rem;
}

.secondary-content p {
  font-size: 0.85rem;
  margin: 0;
}

/* Mobile Responsive */
@media (max-width: 768px) {
  .dashboard-matrix {
    grid-template-columns: 1fr;
    gap: var(--space-xl);
  }

  .elite-performance-card {
    flex-direction: column;
    text-align: center;
    padding: var(--space-md);
  }

  .elite-action {
    text-align: center;
    width: 100%;
  }

  .btn-elite {
    width: 100%;
  }
}